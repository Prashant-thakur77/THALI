# Pinned on 2026-09-15 (Python 3.11, linux x86_64). environment.yml installs this file.
# simulation
mujoco==3.13.0
gymnasium==1.3.0
mink==1.3.0
# training / policies
lerobot==0.4.4
torch==2.10.0
numpy==2.4.6
# openvino stack (CPU + iGPU only on this box — no NPU)
openvino==2026.3.1
openvino-genai==2026.3.1.0
nncf==3.3.0
optimum-intel==2.1.0
# voice
speechmatics-rt==1.1.1
speechmatics-tts==0.1.2
# tooling
pytest==9.1.1
python-dotenv==1.2.3
num2words==0.5.14   # SmolVLM processor dependency (SmolVLA base model load)
matplotlib==3.11.2   # eval/heatmap.py PNG
