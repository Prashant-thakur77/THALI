# Challenge checklist — rubric row → what exists → where

| Rubric row (pts) | Deliverable | Evidence |
|---|---|---|
| Task completion + bimanual (30) | 7-skill dinner task: drawer → fork → spoon **handed A→B via the table** → plate → **B holds mug while A pours** → mug set down | expert {{ frac(load("seeds_expert_expert_test.json")) }} test / {{ frac(load("seeds_expert_expert_train.json")) }} train; ACT policy-only {{ frac(load("seeds_act_policy_only_test.json")) }}, +retry {{ frac(load("seeds_act_policy_retry_test.json")) }}, +fallback {{ frac(load("seeds_act_policy_fallback_test.json")) }} → `results/seeds.json` |
| VLA / multi-modal (20) | per-skill ACT (trained), multi-task SmolVLA ({{ "trained" if load("seeds_smolvla_policy_fallback_test.json") else "pending Kaggle run" }}), local Qwen2-VL-2B INT4 planner on OpenVINO, camera yes/no state check, instruction-swap matrix, replan | `results/planner_eval.json` ({{ load("planner_eval.json")["accepted_from_vlm"] }}/8 VLM, {{ pct(load("planner_eval.json")["verifier_approved_rate"]) }} approved), `results/instruction_swap.json` ({{ load("instruction_swap.json")["correct"] }}/{{ load("instruction_swap.json")["total"] }}), `results/camera_vs_oracle.json` |
| Robustness (15) | 6-axis randomiser, held-out test ranges (+2 textures, +1 mug shape), seeds × axis heatmap, policy-only / retry / fallback | `results/heatmap_expert.json` (hardest: {{ load("heatmap_expert.json")["hardest_axis"] if load("heatmap_expert.json") else "pending" }}), `results/heatmap_act.json`, `results/seeds_*` |
| OpenVINO / Core Ultra (20) | ACT → IR fp32/fp16 + NNCF int8, CPU + iGPU latency, 10-seed preservation per precision, VLM tok/s | `results/bench.json` ({{ ", ".join(f"{k} {v['mean_p50_ms']} ms" for k, v in load("bench.json")["summary"].items()) if load("bench.json") else "pending" }}), `results/preserve.json`, `results/ir_export.json`. **No NPU here**; static shapes ready for `-d NPU`. |
| Reproducibility (10) | `Makefile` (demos/train/eval/bench/demo/verify-log/test), `Dockerfile`, `environment.yml`+`requirements.txt` pins, CI, seeded env, dataset+checkpoints (HF push pending token) | `tests/` ({{ "see CI" }}), `docs/KAGGLE_TODO.md` |
| Innovation (5) | verifier with reach/precondition/workspace/order rules + hash-chained audit log; speaker-focused barge-in | `results/verifier_injection.json` ({{ load("verifier_injection.json")["caught"] }}/20), `results/audit.jsonl`, `results/demo_bargein_stop.json` |
| Speechmatics bonus | realtime, partials, end-of-turn 0.6 s, speaker diarization → speaker focus, custom vocab, `hi` session, TTS loop, barge-in, latency | `results/voice_test.json`, `results/demo_seed3.json` ({{ load("demo_seed3.json")["latency"]["speech_end_to_arm_moves_s"] }} s speech-end → arm-moves) |
