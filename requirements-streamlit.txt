# Thali Live on Streamlit Community Cloud (~1 GB): simulator, expert, rule planner, verifier, queues, audit log, Speechmatics.
# No torch / lerobot / openvino here: the VLM planner and the PatchCore check are not available on this host.
streamlit>=1.38
mujoco==3.13.0
mink==1.3.0
gymnasium==1.3.0
numpy==2.4.6
pillow
jsonschema
python-dotenv
speechmatics-rt==1.1.1
fastapi>=0.115
pydantic>=2
